export class ShopParams{
    //initializing the genreIdSelected variable
genreId = 0;

//initializing the deviceIdSelected variable
deviceId = 0;

//by default, we will sort by name
sort= "name";

search!: string;

pageNumber =1;

pageSize = 6;
}